package blix.logger
{
   public class TraceTarget extends LogTarget
   {
      
      public function TraceTarget(param1:ILoggingManager)
      {
         super(param1);
      }
      
      override protected function logMessage(param1:String, param2:uint) : void
      {
      }
   }
}
